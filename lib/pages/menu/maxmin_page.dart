import 'package:flutter/material.dart';

class MaxminPage extends StatelessWidget {
  const MaxminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
